// The application shell — routes, the terminal host, and the UI kit — arrives
// with its own track. This is the placeholder that keeps the build honest until
// then: a real component, rendered by a real entry point, with nothing invented
// about how the product looks.
export function App()
{
    return <div id="app" />;
}
